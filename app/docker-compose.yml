version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev

  # Future services can be added here
  # For example, a database service:
  # db:
  #   image: postgres:13
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     - POSTGRES_DB=mydb
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password

  # Prism mock server for frontend development:
  # prism:
  #   image: stoplight/prism:4
  #   command: mock -h 0.0.0.0 /path/to/openapi.yaml
  #   ports:
  #     - "4010:4010"
  #   volumes:
  #     - ./docs:/path/to
